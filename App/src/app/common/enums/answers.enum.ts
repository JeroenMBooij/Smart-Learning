export enum AnswerOption {
    Type,
    Write,
    Speech,
    Show
}

export enum FeedbackOption {
    TimeBased,
    InputBased,
    Both
}

export enum Feedback {
    Again,
    Hard,
    Good,
    Easy
}